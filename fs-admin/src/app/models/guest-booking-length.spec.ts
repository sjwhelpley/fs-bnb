import { GuestBookingLength } from './guest-booking-length';

describe('GuestBookingLength', () => {
  it('should create an instance', () => {
    expect(new GuestBookingLength()).toBeTruthy();
  });
});
